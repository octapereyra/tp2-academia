@model Business.Entities.Materia
@{
    ViewData["Title"] = "Eliminar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="mt-2">Materias</h1>
<ol class="breadcrumb mb-4">
    <li class="breadcrumb-item">Entidades</li>
    <li class="breadcrumb-item">Materias</li>
    <li class="breadcrumb-item active">Eliminar</li>
</ol>

<div class="card">
    <div class="card-header">
        <i class="fas fa-book me-1"></i> Eliminar Materia
    </div>
    <div class="card-body">
        <div class="alert alert-danger">
            ¿Desea eliminar la materia <b>@Html.DisplayTextFor(m => m.Descripcion)</b>?
        </div>
        <form action="@Url.Action("Eliminar","Materia")" method="post">
            <input type="hidden" class="form-control" name="ID" value="@Html.DisplayTextFor(m => m.ID)" />

            <button type="submit" class="btn btn-danger">Eliminar</button>
            <a class="btn btn-warning" href="@Url.Action("Materias","Home")">Volver</a>
        </form>

    </div>
</div>

